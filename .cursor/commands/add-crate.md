---
description: Add a MiniRust workspace crate or app without speculative scaffolding
---

Follow the add-minirust-crate skill.

Inspect the workspace first. Create only the crate this task needs. Use `cargo new` and workspace dependencies. Keep `core` and `services` free of infrastructure SDKs. Validate with the validate command when done.

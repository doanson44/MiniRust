---
description: Implement a feature using MiniRust layering
---

Follow the implement-minirust-feature skill.

Place UI in `apps/web`, REST in `apps/api`, rules in `crates/services`, types in `crates/core`. Do not implement unrequested platform features. Validate before finishing.

# Optional local infrastructure. Not required to compile or run the baseline.
#
# Start:
#   docker compose --profile infra up -d
#
# Stop:
#   docker compose --profile infra down
#
# MariaDB is available at localhost:3306.
# Redis is available at redis://127.0.0.1:6379/
#
# The API and web applications do not connect to these services yet.

services:
  redis:
    image: redis:7-alpine
    profiles: ["infra"]
    ports:
      - "6379:6379"
    command: ["redis-server", "--save", "", "--appendonly", "no"]

  mariadb:
    image: mariadb:11
    profiles: ["infra"]
    ports:
      - "3306:3306"
    environment:
      MARIADB_DATABASE: minirust
      MARIADB_USER: minirust
      MARIADB_PASSWORD: ${MARIADB_PASSWORD:-}
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD:-}
